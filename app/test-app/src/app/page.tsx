import { css } from "../../styled-system/css"

export default function Home() {
  return (
    <main
      className={css({
        backgroundColor: "brands.accent",
      })}
    >
      test APP
    </main>
  )
}
