import { styled } from "@qotum/panda-exports/jsx";
import { headline, subtitle, text, title } from "@qotum/panda-exports/recipes";

export const Headline = styled("h1", headline);

export const Title = styled("h1", title);

export const Subtitle = styled("h2", subtitle);

export const Text = styled("p", text);
